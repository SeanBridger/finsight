[pytest]
markers =
    regex: Layer 1 — regex input validation (no AWS needed)
    guardrail: Layer 2 — ApplyGuardrail API (needs guardrail deployed)
    e2e: Layer 3 — full agent stream (needs Fargate running)
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:botocore